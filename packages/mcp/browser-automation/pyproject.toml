[project]
name = "browser-automation-mcp"
version = "0.0.4"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "browser-use>=0.12.0",
    "playwright>=1.40.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
